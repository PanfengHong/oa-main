# 统一 Mock 开关
# 本地开发默认走真实后端（不在此处设置 VITE_USE_MOCK）。
# 如需本地用模拟数据，在 .env.local 里写 VITE_USE_MOCK=true（.env.local 不提交）。
# 生产构建（GitHub Pages 部署）由 .env.production 设置 VITE_USE_MOCK=true 启用 mock。
# VITE_USE_MOCK=true

# 真实接口 baseURL（mock 关闭时生效，开发可指向本地后端）
# VITE_API_BASE_URL=http://localhost:3000

# GitHub Pages 子路径（仓库名），如仓库名为 zdy-oa 则填 /zdy-oa/
# 部署时由 workflow 注入，本地开发留空即可
# VITE_BASE_PATH=/
